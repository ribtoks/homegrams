function y = UserFunction1Exact(x)

	y = -2/x;

%	sqrt2 = sqrt(2);
%	c = log( abs((1-sqrt2) / (1 + sqrt2)) ) / (2*sqrt2);
	
%	if (x > sqrt2)
%		y = sqrt2 + 2*sqrt2 / ( exp(-2*sqrt2*(x + c)) - 1);
%	else
%		y = sqrt2 + 2*sqrt2 / ( -exp(-2*sqrt2*(x + c)) - 1);
%	end
end